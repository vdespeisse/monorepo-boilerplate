{
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "test": "npm run test -ws --if-present && vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=8"
  },
  "devDependencies": {
    "@types/node": "^22.15.24",
    "@vitest/coverage-v8": "^3.1.4",
    "dotenv": "^16.5.0",
    "tsdown": "^0.21.4",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
